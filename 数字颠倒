输入一个整数，将这个整数以字符串的形式逆序输出


#include<bits/stdc++.h>
using namespace std;
int main(){
    int nums;       
    cin>>nums;
    string str=to_string(nums);             //转成字符串
    reverse(str.begin(),str.end());         //你转字符串
    cout<<str;
}
